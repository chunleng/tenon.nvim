## Purpose
Write or update documentation directly in target file

## Process
1. Determine context for this iteration:
   - First iteration (from Set Goal move) → follow goal and structure from Set Goal
   - Returning iteration (from Check Goal move) → check for gaps in choreo memory and prioritize fixing those gaps
2. Apply structure confirmed in previous move:
   - For new docs: create file with outlined sections
   - For updates: modify existing content as planned
3. Write content and edit target file directly:
   - Use gathered context materials
   - Follow documentation conventions (markdown formatting, headings)

## Choreo Move Artifact
None (document modified in place)
